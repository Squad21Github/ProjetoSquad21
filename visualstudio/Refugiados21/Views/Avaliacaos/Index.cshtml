@model IEnumerable<Refugiados1.Models.Avaliacao>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Avaliacoes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Avaliacoes)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAvaliacao">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAvaliacao">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAvaliacao">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
